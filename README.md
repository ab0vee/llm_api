# LLM Chat Console

Консольное приложение для общения с различными LLM моделями через OpenRouter API и Groq API.

## Описание

Простое интерактивное приложение на Python, которое позволяет общаться с различными языковыми моделями через сервисы OpenRouter и Groq. Приложение работает в консоли и выводит ответы модели в реальном времени.

## Поддерживаемые сервисы и модели

### OpenRouter

- 🤖 `anthropic/claude-sonnet-4.5` - Claude Sonnet 4.5 от Anthropic
- 🔍 `perplexity/sonar-deep-research` - Perplexity Sonar Deep Research

### Groq

- 🚀 `qwen/qwen3-32b` - Модель Qwen 3 32B от Alibaba Cloud


## Требования

- Python 3.7+
- Аккаунт на [Groq.com](https://groq.com) с API ключом
- Аккаунт на [OpenRouter.ai](https://openrouter.ai) с API ключом

## Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/ab0vee/llm_api.git
cd llm_api
```

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

## Использование

### OpenRouter

Запустите приложение для работы с OpenRouter:

```bash
# Claude Sonnet 4.5
python openrouter(sonnet).py
```

или

```bash
# Perplexity Sonar Deep Research
python openrouter(perplexity).py
```

### Groq

Запустите приложение для работы с Groq (модель Qwen):

```bash
python groq(qwen).py
```

После запуска вы увидите приглашение для ввода сообщений. Введите ваш вопрос или сообщение, и модель ответит вам.

### Пример сессии:

**OpenRouter (Claude Sonnet 4.5):**

```
LLM API - Модель: anthropic/claude-sonnet-4.5
Введите 'exit' или 'quit' для выхода

Вы: Привет! Расскажи про Python
Ожидание ответа...

Модель: Python — это высокоуровневый язык программирования...

Вы: exit
До свидания!
```

**Groq (Qwen):**

```
LLM API - Модель: qwen/qwen3-32b
Введите 'exit' или 'quit' для выхода

Вы: Привет! Расскажи про Python
Ожидание ответа...

Модель: Python — это высокоуровневый язык программирования...

Вы: exit
До свидания!
```

### Выход из приложения

Для выхода введите одно из следующих слов:

- `exit`
- `quit`
- `выход`


### Файлы конфигурации:

- `openrouter(sonnet).py` - настройки для Claude Sonnet 4.5
- `openrouter(perplexity).py` - настройки для Perplexity Sonar Deep Research
- `groq(qwen).py` - настройки для Qwen 3 32B

## Получение API ключей

### OpenRouter API ключ

1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai)
2. Перейдите в настройки аккаунта
3. Создайте API ключ

### Groq API ключ

1. Зарегистрируйтесь на [Groq.com](https://groq.com)
2. Перейдите в раздел (https://console.groq.com/keys)
3. Создайте новый API ключ или используйте ключ по умолчанию в коде 
4. Скопируйте ключ и установите его в переменную

## Зависимости

- `openai==1.3.0` - библиотека для работы с OpenAI API (совместима с OpenRouter)
- `groq` - официальная библиотека для работы с Groq API

## Особенности

### OpenRouter

- Ограничение максимального количества токенов в ответе (`max_tokens=4000`) для предотвращения превышения лимита кредитов
- Скорость ответа зависит от загрузки серверов OpenRouter

## Ограничения 
<h2>ТРЕБУЕТСЯ VPN!</h2>
- Требуется активное интернет-соединение
- Для OpenRouter необходим баланс кредитов
- Groq имеет свои лимиты на количество запросов в минуту
- Скорость ответа зависит от загрузки серверов выбранного сервиса

## Устранение неполадок

### Ошибка 401 "User not found"

Эта ошибка означает, что API ключ неверный или истек. Проверьте:

- Правильность API ключа в коде или переменной окружения
- Активность вашего аккаунта на соответствующем сервисе
- Наличие достаточного баланса кредитов (для OpenRouter)

### Ошибка импорта библиотеки groq

Убедитесь, что установлены все зависимости:

```bash
pip install -r requirements.txt
```
