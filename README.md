# LLM Chat Console

Консольное приложение для общения с моделью Claude Sonnet 4.5 через OpenRouter API.

## Описание

Простое интерактивное приложение на Python, которое позволяет общаться с языковой моделью Anthropic Claude Sonnet 4.5 через сервис OpenRouter. Приложение работает в консоли и выводит ответы модели в реальном времени.

## Возможности

- 💬 Интерактивный чат с LLM моделью
- 🤖 Использование модели `anthropic/claude-sonnet-4.5`
- 🔒 Поддержка переменных окружения для API ключа
- ⚙️ Настраиваемые параметры (max_tokens, system prompt)

## Требования

- Python 3.7+
- Аккаунт на [OpenRouter.ai](https://openrouter.ai) с API ключом
- Кредиты на OpenRouter для использования модели

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/llm_api.git
cd llm_api
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. (Рекомендуется) Установите переменную окружения для API ключа:
```bash
# Windows PowerShell
$env:OPENROUTER_API_KEY="your-api-key-here"

# Windows CMD
set OPENROUTER_API_KEY=your-api-key-here

# Linux/Mac
export OPENROUTER_API_KEY="your-api-key-here"
```

**Важно:** Если не установить переменную окружения, будет использован ключ по умолчанию из кода. Для безопасности рекомендуется использовать переменную окружения.

## Использование

Запустите приложение:
```bash
python main.py
```

После запуска вы увидите приглашение для ввода сообщений. Введите ваш вопрос или сообщение, и модель ответит вам.

### Пример сессии:

```
LLM API - Модель: anthropic/claude-sonnet-4.5
Введите 'exit' или 'quit' для выхода

Вы: Привет! Расскажи про Python
Ожидание ответа...

Модель: Python — это высокоуровневый язык программирования...

Вы: exit
До свидания!
```

### Выход из приложения

Для выхода введите одно из следующих слов:
- `exit`
- `quit`
- `выход`

## Настройка

Вы можете изменить параметры в файле `main.py`:

- `MODEL_NAME` - название модели (по умолчанию: `anthropic/claude-sonnet-4.5`)
- `SYSTEM_PROMPT` - системный промпт для модели
- `MAX_TOKENS` - максимальное количество токенов в ответе (по умолчанию: 4000)

## Получение API ключа

1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai)
2. Перейдите в настройки аккаунта
3. Создайте API ключ
4. Пополните баланс кредитов для использования моделей

## Структура проекта

```
llm_api/
├── main.py              # Основной файл приложения
├── requirements.txt     # Зависимости проекта
├── README.md           # Документация
└── .gitignore          # Игнорируемые файлы для Git
```

## Зависимости

- `openai` - библиотека для работы с OpenAI API (совместима с OpenRouter)

## Ограничения

- Приложение ограничивает максимальное количество токенов в ответе (`max_tokens=4000`) для предотвращения превышения лимита кредитов
- Скорость ответа зависит от загрузки серверов OpenRouter
- Требуется активное интернет-соединение

## Лицензия

Этот проект распространяется под лицензией MI
